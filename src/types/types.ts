type FormType = {
  email: string;
  password: string;
  visual: boolean;
  token: string
};

export default FormType

export type columnsTable = {
key: string,
dataIndex: string,
title: string
}

export type dataSourceTableSales = {
  key: string,
  name: string,
  group: string,
  category: string,
  podcategory: string,
  sku: string,
}